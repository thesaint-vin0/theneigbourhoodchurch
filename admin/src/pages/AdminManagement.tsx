import { FormEvent, useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
import { useAuth } from '../context/AuthContext'
import type { Profile } from '../types'
export default function AdminManagement(){const{profile}=useAuth();const[admins,setAdmins]=useState<Profile[]>([]);const[email,setEmail]=useState('');const[name,setName]=useState('');const[password,setPassword]=useState('');const[role,setRole]=useState('admin');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function load(){const{data}=await supabase.from('profiles').select('*').in('role',['super_admin','admin','editor']).order('created_at');setAdmins((data||[]) as Profile[])}useEffect(()=>{load()},[]);async function create(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');const{data,error}=await supabase.functions.invoke('create-admin',{body:{email,password,full_name:name,role}});setBusy(false);if(error||data?.error){setMsg(error?.message||data?.error||'Unable to create administrator');return}setMsg('Administrator created successfully.');setEmail('');setName('');setPassword('');await load()}if(profile?.role!=='super_admin')return <div><h1 className="text-2xl font-bold">Admin Management</h1><p className="text-slate-500 mt-2">Only the super administrator can add or manage administrators.</p></div>;return <div><h1 className="text-2xl font-bold">Admin Management</h1><p className="text-slate-500 mt-1">Create controlled administrator accounts without exposing the Supabase service-role key.</p><div className="grid lg:grid-cols-2 gap-6 mt-6"><form onSubmit={create} className="bg-white border rounded-xl p-6 space-y-4"><h2 className="font-semibold">Add administrator</h2>{msg&&<div className="p-3 rounded-lg bg-slate-50 text-sm">{msg}</div>}<input required type="text" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} className="w-full border rounded-lg px-3 py-2.5"/><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full border rounded-lg px-3 py-2.5"/><input required minLength={8} type="password" placeholder="Temporary password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)} className="w-full border rounded-lg px-3 py-2.5"/><select value={role} onChange={e=>setRole(e.target.value)} className="w-full border rounded-lg px-3 py-2.5"><option value="admin">Admin</option><option value="editor">Editor</option></select><button disabled={busy} className="w-full bg-blue-700 text-white rounded-lg py-2.5 disabled:opacity-50">{busy?'Creating…':'Create administrator'}</button></form><div className="bg-white border rounded-xl p-6"><h2 className="font-semibold mb-4">Current administrators</h2><div className="space-y-3">{admins.map(a=><div key={a.id} className="border rounded-lg p-3"><div className="font-medium">{a.full_name||'Unnamed administrator'}</div><div className="text-sm text-slate-500">{a.email} · {a.role}</div></div>)}</div></div></div></div>}
