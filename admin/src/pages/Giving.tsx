import { useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
import type { GivingTransaction } from '../types'
export default function Giving(){const [rows,setRows]=useState<GivingTransaction[]>([]);useEffect(()=>{(async()=>{const {data}=await supabase.from('giving_transactions').select('*').order('created_at',{ascending:false}).limit(200);setRows((data??[]) as GivingTransaction[]);})()},[]);const ngn=rows.filter(x=>x.currency==='NGN'&&x.status==='success').reduce((a,x)=>a+Number(x.amount),0);return <div><h1 className="text-2xl font-bold">Giving</h1><p className="text-slate-500 mt-1">Paystack transactions and giving history.</p><div className="grid sm:grid-cols-3 gap-4 my-5"><div className="bg-white border rounded-xl p-5"><div className="text-sm text-slate-500">Successful NGN</div><div className="text-2xl font-bold mt-2">₦{ngn.toLocaleString()}</div></div><div className="bg-white border rounded-xl p-5"><div className="text-sm text-slate-500">Transactions</div><div className="text-2xl font-bold mt-2">{rows.length}</div></div></div><div className="bg-white border rounded-xl overflow-auto"><table className="w-full text-sm"><thead className="bg-slate-50"><tr><th className="p-3 text-left">Date</th><th className="p-3 text-left">Name</th><th className="p-3 text-left">Amount</th><th className="p-3 text-left">Currency</th><th className="p-3 text-left">Fund</th><th className="p-3 text-left">Status</th><th className="p-3 text-left">Reference</th></tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-t"><td className="p-3">{new Date(x.created_at).toLocaleString()}</td><td className="p-3">{x.name||'—'}</td><td className="p-3">{Number(x.amount).toLocaleString()}</td><td className="p-3">{x.currency}</td><td className="p-3">{x.fund}</td><td className="p-3">{x.status}</td><td className="p-3">{x.reference||'—'}</td></tr>)}</tbody></table></div></div>}
