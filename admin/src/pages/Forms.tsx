import { useEffect, useState } from 'react'
import { supabase } from '../lib/supabase'
const tabs=['prayer_requests','contact_messages','volunteer_signups','event_registrations','newsletter_subscribers','plan_your_visit']
export default function Forms(){const [tab,setTab]=useState(tabs[0]);const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabase.from(tab).select('*').order('created_at',{ascending:false}).limit(100);setRows(data??[])})()},[tab]);return <div><h1 className="text-2xl font-bold">Forms & submissions</h1><p className="text-slate-500 mt-1 mb-5">Private submissions are visible to authorized administrators only.</p><div className="flex flex-wrap gap-2 mb-5">{tabs.map(x=><button key={x} onClick={()=>setTab(x)} className={`px-3 py-2 rounded-lg text-sm ${tab===x?'bg-blue-700 text-white':'bg-white border'}`}>{x.replaceAll('_',' ')}</button>)}</div><div className="bg-white border rounded-xl overflow-auto"><table className="w-full text-sm"><thead className="bg-slate-50"><tr>{Object.keys(rows[0]??{id:'ID'}).map(k=><th key={k} className="text-left p-3 whitespace-nowrap">{k}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.id??i} className="border-t">{Object.entries(r).map(([k,v])=><td key={k} className="p-3 max-w-xs align-top">{typeof v==='string'&&v.length>180?v.slice(0,180)+'…':String(v??'')}</td>)}</tr>)}</tbody></table>{!rows.length&&<div className="p-8 text-center text-slate-500">No records.</div>}</div></div>}
