import { FormEvent, useEffect, useState } from 'react'
import { Pencil, Plus, Trash2 } from 'lucide-react'
import { supabase } from '../lib/supabase'
import type { ChurchEvent } from '../types'
const empty={title:'',date:'',end_date:'',time:'',location:'',description:'',image_url:'',category:'',registration_required:false,registration_enabled:false,published:true}
export default function Events(){const [items,setItems]=useState<ChurchEvent[]>([]);const [form,setForm]=useState<any>(empty);const [editing,setEditing]=useState<string|null>(null);const [open,setOpen]=useState(false);async function load(){const {data}=await supabase.from('events').select('*').order('date',{ascending:true});setItems((data??[]) as ChurchEvent[])}useEffect(()=>{load()},[]);async function save(e:FormEvent){e.preventDefault();const payload={...form,image_url:form.image_url||null,end_date:form.end_date||null};if(editing)await supabase.from('events').update(payload).eq('id',editing);else await supabase.from('events').insert(payload);setOpen(false);setEditing(null);setForm(empty);await load()}async function del(id:string){if(confirm('Delete this event?')){await supabase.from('events').delete().eq('id',id);await load()}}return <div><div className="flex justify-between items-center mb-6"><div><h1 className="text-2xl font-bold">Events</h1><p className="text-slate-500">Create events and control their countdown/registration data.</p></div><button onClick={()=>{setForm(empty);setEditing(null);setOpen(true)}} className="bg-blue-700 text-white px-4 py-2 rounded-lg flex gap-2 items-center"><Plus size={18}/> Add event</button></div><div className="grid gap-4">{items.map(x=><div key={x.id} className="bg-white border rounded-xl p-5 flex justify-between gap-4"><div><h2 className="font-semibold">{x.title}</h2><p className="text-sm text-slate-500 mt-1">{x.date} · {x.time} · {x.location}</p><p className="text-sm mt-2">{x.published?'Published':'Draft'} · {x.registration_enabled?'Registration on':'Registration off'}</p></div><div className="flex gap-3"><button onClick={()=>{setForm({...x});setEditing(x.id);setOpen(true)}}><Pencil size={18}/></button><button onClick={()=>del(x.id)}><Trash2 size={18}/></button></div></div>)}</div>{open&&<div className="fixed inset-0 bg-black/40 z-50 p-4 overflow-auto"><form onSubmit={save} className="bg-white max-w-2xl mx-auto my-8 rounded-2xl p-6 space-y-3"><h2 className="text-xl font-bold">{editing?'Edit':'Add'} event</h2>{[['title','Title'],['date','Start date'],['end_date','End date'],['time','Time'],['location','Location'],['category','Category'],['image_url','Image URL']].map(([k,l])=><label key={k} className="block text-sm">{l}<input className="mt-1 w-full border rounded-lg px-3 py-2" type={k.includes('date')?'date':'text'} value={form[k]??''} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label className="block text-sm">Description<textarea className="mt-1 w-full border rounded-lg px-3 py-2" rows={4} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><div className="flex flex-wrap gap-5"><label><input type="checkbox" checked={!!form.registration_required} onChange={e=>setForm({...form,registration_required:e.target.checked})}/> Registration required</label><label><input type="checkbox" checked={!!form.registration_enabled} onChange={e=>setForm({...form,registration_enabled:e.target.checked})}/> Registration enabled</label><label><input type="checkbox" checked={!!form.published} onChange={e=>setForm({...form,published:e.target.checked})}/> Published</label></div><div className="flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)} className="px-4 py-2 border rounded-lg">Cancel</button><button className="px-4 py-2 bg-blue-700 text-white rounded-lg">Save</button></div></form></div>}</div>}
