import { useAuth } from '../context/AuthContext'
export default function Settings(){const {profile}=useAuth();return <div><h1 className="text-2xl font-bold">Settings</h1><div className="mt-6 bg-white border rounded-xl p-6 max-w-xl"><h2 className="font-semibold">Administrator</h2><div className="mt-4 grid gap-3 text-sm"><div><span className="text-slate-500">Email:</span> {profile?.email}</div><div><span className="text-slate-500">Role:</span> {profile?.role}</div></div><p className="mt-6 text-sm text-slate-500">User management and password reset can be added after the first super-admin account is created in Supabase Auth.</p></div></div>}
