import { FormEvent, useEffect, useState } from 'react'
import { Save } from 'lucide-react'
import { supabase } from '../lib/supabase'

const defaults={church_name:'The Neighbourhood Church',tagline:'',email:'',phone:'',address:'',facebook:'',instagram:'',youtube:'',giving_ngn_enabled:true,giving_usd_enabled:true,default_giving_currency:'NGN',default_event_registration_enabled:true}
export default function Settings(){const [settings,setSettings]=useState<any>(defaults);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[msg,setMsg]=useState('');useEffect(()=>{(async()=>{const{data}=await supabase.from('site_settings').select('key,value');const next={...defaults};for(const row of data||[])next[row.key]=row.value;setSettings(next);setLoading(false)})()},[]);function set(k:string,v:any){setSettings((s:any)=>({...s,[k]:v}))}async function save(e:FormEvent){e.preventDefault();setSaving(true);setMsg('');try{for(const key of Object.keys(defaults)){const{error}=await supabase.from('site_settings').upsert({key,value:settings[key],updated_at:new Date().toISOString()});if(error)throw error}setMsg('Settings saved successfully.')}catch(err){setMsg(err instanceof Error?err.message:'Unable to save settings')}finally{setSaving(false)}}if(loading)return <div>Loading settings…</div>;return <div><h1 className="text-2xl font-bold">Settings</h1><p className="text-slate-500 mt-1">Manage the church information used by the CMS.</p><form onSubmit={save} className="mt-6 max-w-3xl space-y-5"><section className="bg-white border rounded-xl p-6 space-y-4"><h2 className="font-semibold">Church information</h2>{[['church_name','Church name'],['tagline','Tagline'],['email','Contact email'],['phone','Phone'],['address','Address']].map(([k,l])=><label key={k} className="block text-sm">{l}<input className="mt-1 w-full border rounded-lg px-3 py-2.5" value={settings[k]??''} onChange={e=>set(k,e.target.value)}/></label>)}</section><section className="bg-white border rounded-xl p-6 space-y-4"><h2 className="font-semibold">Social links</h2>{[['facebook','Facebook URL'],['instagram','Instagram URL'],['youtube','YouTube URL']].map(([k,l])=><label key={k} className="block text-sm">{l}<input className="mt-1 w-full border rounded-lg px-3 py-2.5" value={settings[k]??''} onChange={e=>set(k,e.target.value)}/></label>)}</section><section className="bg-white border rounded-xl p-6 space-y-4"><h2 className="font-semibold">Giving & registration</h2><label className="flex gap-2 items-center text-sm"><input type="checkbox" checked={!!settings.giving_ngn_enabled} onChange={e=>set('giving_ngn_enabled',e.target.checked)}/> Enable ₦ Naira giving</label><label className="flex gap-2 items-center text-sm"><input type="checkbox" checked={!!settings.giving_usd_enabled} onChange={e=>set('giving_usd_enabled',e.target.checked)}/> Enable $ USD giving</label><label className="block text-sm">Default giving currency<select className="mt-1 w-full border rounded-lg px-3 py-2.5" value={settings.default_giving_currency} onChange={e=>set('default_giving_currency',e.target.value)}><option>NGN</option><option>USD</option></select></label><label className="flex gap-2 items-center text-sm"><input type="checkbox" checked={!!settings.default_event_registration_enabled} onChange={e=>set('default_event_registration_enabled',e.target.checked)}/> Enable event registration by default</label></section>{msg&&<div className="p-3 rounded-lg border bg-slate-50 text-sm">{msg}</div>}<button disabled={saving} className="bg-blue-700 text-white px-4 py-2.5 rounded-lg flex gap-2 items-center disabled:opacity-50"><Save size={17}/>{saving?'Saving…':'Save settings'}</button></form></div>}
